# 完成事项</font>
+ 环境配置
+ 博客完善
# 下周待做事项</font>
+ 决战ISCTF
# 本周学习的知识分享</font>
## 小东西
+ 单引号 '' 包裹的字符常量在程序编译时就会被转换为对应的 ASCII 整数值
+ 指针==地址
+ strlen从给定的指针开始,逐个字节检查,直到遇到第一个值为0的字节,然后返回之前统计的字符个数(不包括本身)python里不是这样
+ 二进制在计算式总是先给有符号整数取补码
## easyre
+ ida中打开发现看不懂,啃了半天遗憾退场.........
+ 才怪嘞,不知道在干啥就先启动程序看一下
+ Very easy upx!
Then flag?
+ 啊?有加密的吗?那为什么打开不是乱码?
+ 考虑到这个题确实在我的桌面上有一段时间了,确实需要想一下难道我是什么时候脱壳了?
+ 那就发给deepseek看看
+ 这段代码是典型的 加壳/混淆代码的反编译结果，很可能是由 UPX、ASPack 等加壳工具生成的解压/解密代码。让我帮你分析一下：
+ 好家伙,原来加密完的函数不一定会变成数据团,还有可能变成混淆后的函数形态啊
+ 那就脱壳之后再看一下
```
int __fastcall main(int argc, const char **argv, const char **envp)
{
  _main();
  puts("Very easy upx!");
  puts("Then flag?");
  system("pause");
  return 0;
}
```
+ 问我flag在哪,说明可能是寻找类题目,开搜
+ 一抬头就看见明晃晃的.data:0000000000403010	0000000F	C	d_0n3_4nd_tw0}
+ 点看一看part2,那就搜搜part1
+ 点进去就是个return 0x5850557B67616C66i64
+ 按r显示XPU{galf
+ 怎么看着有点像flag{UPX
+ 拼在一起上传一下,答案错误
+ 那我没招了,搜一下wp,明明是一样的啊,有点抽象
+ 不过这里多留意了一下为什么会倒过来,看了好几个wp,问了ai,除了几个水货之外得到的统一结论就是x86/x64默认使用小端序,而且面向结果来看这里应该分析出来是倒序,纯搞呢吧
## toddler_regs
+ 这byd难度1真的把我折磨得好惨啊
+ 这道题给了两个文件,一个exe和一个pdb文件
+ pdb?这个我熟啊,好像是记录生物蛋白核酸序列的来着?放在这干什么?要考我最擅长的生物了吗
+ 尝试打开无果后,愚蠢的我选择没有理会它,把它当作了协同文件一类的东西,和exe一起选中放进了ida里
+ 然后我很快付出了代价,反编译出了一堆我完全看不懂的字符堆,我搜了半天也是什么都没有
+ 所以接下来我单独反编译了exe,这回终于找到了
```
void __noreturn stage_wrapper()  //表示此函数会一直进行,不返回
{
  j_split();       //分界线
  puts("Follow the tasks below and you will get flag");
  j_split();       //再次展示
  puts("[+] Task 1");
  puts("[+] Description: Set the argument of the next function called equal to 23");
  j_printf("[+] Type Enter to continue");
  getchar();
  j_stage_1(32);
  j_split();
  puts("[+] Task 2");
  j_printf("[+] Description: Set the next function called is stage_2_real %p\n", j_stage_2_real);
  j_printf("[+] Type Enter to continue");
  getchar();
  j_stage_2_fake();
}
```
+ 那我们就一步步follow它的task吧
+ 观察到它给我们下达一个改数值的任务,结合打开题目之后显示的内容:how to debug,我们猜测这里应该是要动态调试一下
+ 启动之后发现停留在一个奇怪的页面,我们直接搜索flag尝试定位过来
+ 看到一个20h,尝试将其改成17h
+ 然而我陷入了迷茫,找不到Modify value,按ctrl+e也没用
+ 紧接着我又遇见更迷茫的事了
```
.rdata:000000014001B2D8 aTask2Checking db '[+] Task 2 checking ',0
.rdata:000000014001B2D8                                         ; DATA XREF: stage_2_fake+1A↑o
.rdata:000000014001B2D8                                         ; stage_2_real+1A↑o
.rdata:000000014001B2ED db    0
.rdata:000000014001B2EE db    0
.rdata:000000014001B2EF db    0
.rdata:000000014001B2F0 db    0
.rdata:000000014001B2F1 db    0
.rdata:000000014001B2F2 db    0
.rdata:000000014001B2F3 db    0
.rdata:000000014001B2F4 ; const char Source[]
.rdata:000000014001B2F4 Source db 'flag{',0                     ; DATA XREF: stage_2_real+6D↑o
.rdata:000000014001B2FA align 20h
.rdata:000000014001B300 ; const char a1sN1c3But[]
.rdata:000000014001B300 a1sN1c3But db '_1s_n1c3_but_',0         ; DATA XREF: stage_2_real+A7↑o
.rdata:000000014001B30E align 10h
.rdata:000000014001B310 ; const char aIsWe1rd[]
.rdata:000000014001B310 aIsWe1rd db '_is_we1rd}',0              ; DATA XREF: stage_2_real+E1↑o
.rdata:000000014001B31B align 20h
.rdata:000000014001B320 ; const char aSuccessTask2Fi[]
.rdata:000000014001B320 aSuccessTask2Fi db '[+] Success: Task 2 finished!',0
```
+ 我觉得前途一片黑暗,绝望地往前翻,结果就看见了task2,tmd直接把flag的三段贴出来了
+ 那你让我改集贸呢??????
+ 吓得我赶紧回去看看反编译行间
```
int stage_2_real()
{
  int i; // [rsp+24h] [rbp+4h]

  Sleep(0x5DCu);
  j_printf("[+] Task 2 checking ");
  for ( i = 0; i < 10; ++i )
  {
    Sleep(0x64u);
    j_printf(".");
  }
  puts(byte_14001AC26);
  g_teamjnu_idx = 184;
  j_strcpy_0(flag, "flag{");
  j_strcat_0(flag, team[g_team_idx]);
  j_strcat_0(flag, "_1s_n1c3_but_");
  j_strcat_0(flag, teamjnu[g_teamjnu_idx]);
  j_strcat_0(flag, "_is_we1rd}");
  return puts("[+] Success: Task 2 finished!");
}
```
+ flag = "flag{" + team[g_team_idx] + "_1s_n1c3_but_" + teamjnu[g_teamjnu_idx] + "_is_we1rd}"
+ 找数值
```
char teamjnu[512][10] =
{
  { 'X', 'P', '0', 'I', 'N', 'T', 'J', 'N', 'U', '\0' },
  "XP0INTJNu",
  "XP0INTJnU",
  "XP0INTJnu",
  "XP0INTjNU",
.........................................(太长了省略一下)
  "xpoinTjnu",
  "xpointJNU",
  "xpointJNu",
  "xpointJnU",
  "xpointJnu",
  "xpointjNU",
  "xpointjNu",
  "xpointjnU",
  "xpointjnu"
};
```
```
char team[64][7] =
{
  { 'X', 'P', '0', 'I', 'N', 'T', '\0' },
  "XP0INt",
  "XP0InT",
  "XP0Int",
  "XP0iNT",
  "XP0iNt",
  "XP0inT",
  "XP0int",
  "XPoINT",
....................................................(同)
  "xpoiNT",
  "xpoiNt",
  "xpoinT",
  "xpoint"
};
```
+ 直接找到索引值
+ 复制过来拼拼拼就好了🤡
+ flag{Xp0int_1s_n1c3_but_Xp0intJNU_is_we1rd}
### 但是呢,如果仅仅是这样完成,那它不过是一道非常简单的算法题
+ 我最终决定跟着task走一遍,今天不用修改就能直接找到答案,那下次呢?以后呢?技多不压身
+ 经过高人指点,我绝望地意识到,我刚刚在这找找寻寻磨磨蹭蹭一个多小时是因为tm的版本不对,我真要气死了🤯
+ 但是高人告诉我我这个版本也有做的方法,不用pather,就用patching,从图形更改变成了修改机器码
+ 其实这个界面我乱点出来过,但是当时看见一长串连续16位,下意识地以为是改地址的了
+ B9 20 00 00 00 E8 93 F1 FF FF E8 5C F1 FF FF 48
+                       ↓
+ B9 17 00 00 00 E8 93 F1 FF FF E8 5C F1 FF FF 48
+ 如图进行修改
+ 哦对,顺带提一下,由于我发现上传好像会丢失图片,那就尽量不用图,需要的话就文字写真
```
B9(mov ecx,imm32) 17 00 00 00(0x00000017)    mov ecx, 17h  ; //将23放入ECX寄存器
E8 93 F1 FF FF    call xxxxxxxx  ; //调用某个函数
E8 5C F1 FF FF    call xxxxxxxx  ; //调用另一个函数  
48                ...           ; //可能是其他指令的开始
```
+ E8后面跟的是相对地址(偏移量),而且还是小端格式,原始字节应该是0xFFFFF193
+ 目标地址=下一条指令地址+相对偏移
+ 目标地址是这条命令想去往的地方,不会在这句话对应机器码中出现,而本地地址,也就是ida前面的地址的后一条地址加上E8后面的那些偏移量就能算出目标位置
+ 下一条指令地址就是你现在修改的函数所在的地址的下一个命令的地址,因为这个时候cpu已经走出本地址了,确实是比较好玩
+ 相对偏移就是改变量
+ 说的这些虽然对改数值没什么用(因为更简单),但是却紧紧关联task2
+ 我们接着进行逆向分析
+ Description: Set the next function called is stage_2_real
+ 再观察代码,发现最后一句确实是对fake的调用
+ 跟进检查发现real就是我们刚刚分析出flag的函数,如果检测全部通过应该会直接由real输出答案
+ 所以,我们应该把最后的函数调用改成调用real
+ 正好它给出了real函数,我们跟进去找地址,找到之后按照上面写的方法进行计算,修改这里call的函数的唤取地址,把real调过来
+ 接下来我们就发现函数美美变红了
+ 然后我又去问deepseek,又被它不知所云的废话和无效快捷键折磨了一个半小时后绝望地想去寻求哈纳伏打的帮助,但是一想到他的浩荡淫威我便抖若筛糠,连子抖部辉大了
+ 无奈之下我只能再次去寻求高人帮助,在高人也被我的机革折磨一通之后他直接给了我一个最终办法
+ 把整个函数全选,然后在开头重建,果然成功了!这也太强了!
+ 接下来我又去运行程序,但是问题接踵而至,在local windows debugger中出现的程序快速闪退
+ 我用vc录屏之后拉帧,发现32的值并没有变过来,所以task1没通过,被exit()了
+ 也就是说我没有成功保存
+ 在乱研究许久之后我竟因祸得福找到了那个本来不存在的purchar program,就在edit中
+ 从这里发送文件给桌面,重新运行,终归看到了过前两关的提示,此时我们在终端里看到了一串地址
+ 检索之后找到了flag的定义地,kimi说task3不考别的,只要你过去看
+ 但是完全不对啊,这里的flag是有32个0的50容量空数组
+ 难道是加密了?我只觉得是这样,总不能在这种时候又让我回去分析算法吧???那也太搞了
+ 一筹莫展的我翻看起了和高人的聊天记录,看着他发来调试的图片,一处细节引起了我的注意
+ Description: Read the flag from %s
+ 这里是s,也就是说高人让这里改成字符串,由程序自己识别输出
+ 我第一时间想到要修改这里,打开pur byte,找25 70无果
+ 我又点了好久各种奇怪的东西,没招了,真没招了
+ 但是就在此时,灵光惊乍脑海,旧影妙破眼睑!!
+ 一组在我成功重构函数之后的对话忽然如惊蛰般冲来
+ "我去,太强了吧,什么原理啊?!"
+ "u是unsigned,未定义,p就是c,code,解释为指令"
+ 念已至此,我便不再犹豫,大力敲击u键宣告:未定义!!
+ "啪"的一声!宏图展开!所有被隐藏的内容全部弹出,我一把抓住70,顷刻改为73,再次全选,按下c键
+ 至是,工程已毕,皆结于此
```
******************************************
Follow the tasks below and you will get flag
******************************************
[+] Task 1
[+] Description: Set the argument of the next function called equal to 23
[+] Type Enter to continue
[+] Task 1 checking ..........
[+] Success: Task 1 finished!
******************************************
[+] Task 2
[+] Description: Set the next function called is stage_2_real 000000014001100F
[+] Type Enter to continue
[+] Task 2 checking ..........
[+] Success: Task 2 finished!
******************************************
[+] Task 3
[+] Description: Read the flag from flag{Xp0int_1s_n1c3_but_Xp0intJNU_is_we1rd}
[+] Type enter to continue
```
+ 肖申克的救赎.jpg
+ 回望整道题的历程,实在很难称得上是一道难题,但却在一些方法的研究上让我绞尽脑汁,苦不堪言
+ 以现在的我来看前面很多问题都得到了解释,方法也可以更优化
+ 在fake改为real的调用中可以用program里的assemble模块直接命名搜算,完全不用手动计算省时省力规避风险
+ 而flag之所以为空,答案恰恰就在real函数中,此刻的flag不过是一个空容器,经过前面real的运行之后flag里才被填充了数据,才可以在后面输出
+ 而且exe也不用导出,在左上角保存之后完全可以进行本地调试
+ 这道题,我觉得算是一道方法教学题,而我在这里面可谓是摔得老惨老惨了
+ 我不清楚我摔这么多跤到底有没有用,它比我直接拿到方法去做一遍效果怎么样,但是我确实是发现了
+ 网上好多wp感觉纯纯在水,为了找答案在做题,这道题的本意解法却完全没有人上传,导致我根本找不到一般做法,一直被卡脚
+ 当然也不是说算法完成不好,毕竟这是最快最高效的做法,但是这样却完全违背了本意,失去了这道题的教学用处
+ 要不我把这题的wp传到网上吧?怎么样呢...
## 由此我顺便统计了IDA中快捷键的用法和功能
| 导航类            | 功能               | 例子                                    |
| -------------- | ---------------- | ------------------------------------- |
| **G**          | 跳到任意地址           | `G` → `0x140003000` → 回车              |
| **Enter**      | 跟随跳转（call/jmp）   | 在 `call sub_401000` 上按 `Enter` 跳过去    |
| **Esc**        | 返回上一位置           | 跳过去后按 `Esc` 回到原处                      |
| **Ctrl+z** | 取消操作（可多次）          | 比如我重新构建错了,就`ctrl+z`                            |
| **X**          | 查看引用（谁调用了我）      | 在函数名上按 `X` 列出所有 caller                |
| **Ctrl+X**     | 查看数据引用（谁用了这个字符串） | 在 `"flag{"` 上按 `Ctrl+X`               |
| **Alt+B**      | 二进制搜索（搜字节）       | `Alt+B` → `E8 00 00 00 00` 找 `call 0` |
| **Alt+T**      | 文本搜索（搜字符串）       | `Alt+T` → `password` 搜明文              |

| 分析类       | 功能           | 实例                                              |
| --------- | ------------ | ----------------------------------------------- |
| **N**     | 重命名          | 把 `sub_140001000` 改名 `decrypt`                  |
| **;**     | 添加注释         | 在混淆代码旁写 `; 这个没用`                              |
| **C**     | 强制解析为代码      | 未识别区选中按 `C` 变指令                                 |
| **D**     | 强制解析为数据      | 把误解析的指令 `D` 成 `db 0E8h`                         |
| **A**     | 解析为字符串       | 选中 `48 65 6C 6C 6F` 按 `A` → `"Hello"`           |
| **U**     | 取消解析（退回原始字节） | 修改数据或重建函数                            |
| **P**     | 创建/识别函数      | 在函数头按 `P` 生成 `sub_xxx`                          |
| **Alt+P** | 编辑函数属性       | 不知道有啥用             |
| **Y**     | 设置类型（函数/变量）  | 在函数名按 `Y` 写 `int __cdecl foo(char *)`           |
| **T**     | 应用结构体        | 在 `[rax+18h]` 按 `T` 选 `_PEB` → `PEB.Ldr`        |
| **H**     | 切换数字进制显示     | 按 `H` 把 `1000` 变 `0x3E8`                        |
| **R**     | 切换数字为字符      | 按 `R` 把 `0x41` 变 `'A'`                          |
| **M**     | 循环切换操作数格式    | 反复按 `M` 在 `0x401000`、`4198400`、`sub_401000` 间切换 |

| 调试         | 功能         | 实例                    |
| ----------- | ---------- | --------------------- |
| **F10**     | 单步进行            |在断点处 `F10` 进行下一步
| **F7**      | 单步步入（进函数）  | 在 `call` 上 `F7` 跟进去   |
| **F8**      | 单步步过（跳过函数） | 在 `call` 上 `F8` 不跟进去  |
| **F9**      | 继续运行（直到断点） | 断下后 `F9` 跑起来          |
| **F2**      | 设置/取消断点    | 在指令上 `F2` 下软断         |
| **Ctrl+F7** | 运行到函数返回    | 快速跑出当前函数              |
| **;**（调试时）  | 设置条件断点     | 在断点按 `;` 写 `rax == 0` |

| 视图           | 功能                   |
| ------------- | -------------------- |
| **Space**     | 反汇编 ↔ 控制流图 切换        |
| **F5**        | 打开/刷新伪代码窗口（Hex-Rays） |
| **Tab**       | 在反汇编与伪代码间互切          |
| **Shift+F12** | 打开字符串窗口              |

+ 那么值得一提的是,如果你进行的操作无法ctrl+z,那它可能是破坏性操作,需要用U+C进行重新解析来重置
# 本周学习总结</font>
+ 被搞麻了,基本没学到很多东西
# 杂项</font>
+ 搞了Gemini3.0的api,可以在比赛时吃点好的了(虽然deepseek也待我不薄...)