# 完成事项</font>
+ 写了好多题,还有好多题来不及写上去了
# 下周待做事项</font>
+ 学汇编,复习c语言
+ 有空再做做题,搞一下music吧
# 本周学习的知识分享</font>
## 1000Click
+ 我玩神秘小RPG的外挂也能用来做CTF么?
+ 题目要求点按钮一千下,于是我突发奇想启动了CE修改器,我去,还真行啊
## easyEZbaby_app
+ 接触到安卓逆向了,新生赛的移动模块我基本都做了,就剩下那个压轴题还没看,所以我一直以为IDA应该是能做安卓的,但是结果却是没法解析
![alt text](image-3.png)
+ 还记得最开始我还不知道这个界面是什么意思,现在才知道是不能解析,一般要么是文件损坏,要么就是没有对应的反编译器
+ 然后我就去搞了个jadx,下载完打开解析之后才发现,真正的安卓逆向其实和逆向的区别是很大的
+ 这么想不知道对不对,因为考虑到IDA上打开的安卓逆向其实和普通逆向差距并不大,所以我更感觉其实是软件解析后对函数的分块有所不同,所以我们要采取的措施也有所不同
+ 但是无论如何,关于安卓的大概知识,也是需要补足一下的,所以
### 安卓基础
+ 安卓目前由于内容不多所以偏向于杂项和逆向的考察(我去,怎么刚好两个我都学?那这安卓也太适合我了)
+ 杂项中主要需要根据安卓系统特性寻找隐藏数据,考察对系统特性的熟悉程度
+ 逆向中主要是考察对Java和C++的逆向分析能力

#### 安卓四大组件
+ Activity,安卓界的main函数,是面向用户的应用组件或用户操作的可视化界面,基于Activity基层,底层由ActivityManager统一管理,也负责应用内或应用间的Inter消息
+ BroadcastReceiver,接受并过滤广播消息的组件,作为广播组件可能会显示flag
+ Service,处理后台耗时逻辑,常常被把算法分离到这里
+ ContentProvider,应用程序间数据共享的组件,可能涉及数据库查询问题
+ 这么看来其实和IDA也差不多,主要关注一个"main"函数还有有可能分离计算的service(各种sub函数)
+ 那么接下来就去act函数看看吧
```
    @Override // android.view.View.OnClickListener
    public void onClick(View view) {
        String string = this.username.getText().toString();
        String string2 = this.password.getText().toString();
        if (checkUsername(string) && checkPass(string2)) {
            Toast.makeText(this, "登录成功", 0).show();
            Toast.makeText(this, "flag{" + string + string2 + "}", 0).show();
            return;
        }
        Toast.makeText(this, "登录失败", 0).show();
    }

    public boolean checkUsername(String str1) throws NoSuchAlgorithmException {
        if (str1 != null) {
            try {
                if (str1.length() != 0 && str1 != null) {
                    MessageDigest messageDigest = MessageDigest.getInstance("MD5");
                    messageDigest.reset();
                    messageDigest.update("zhishixuebao".getBytes());
                    String hexString = toHexString(messageDigest.digest(), "");
                    StringBuilder sb = new StringBuilder();
                    for (int i = 0; i < hexString.length(); i += 2) {
                        sb.append(hexString.charAt(i));
                    }
                    return (sb.toString()).equals(str1);
                }
                return false;
            } catch (NoSuchAlgorithmException e) {
                e.printStackTrace();
            }
        }
        return false;
    }

    public boolean checkPass(String str) {
        if (str == null) {
            return false;
        }
        char[] charArray = str.toCharArray();
        if (charArray.length != 15) {
            return false;
        }
        for (int i = 0; i < charArray.length; i++) {
            charArray[i] = (char) ((((255 - i) + 2) - 98) - charArray[i]);
            if (charArray[i] != '0' || i >= 15) {
                return false;
            }
        }
        return true;
    }

```
+ 又到了算法环节了,不过这里的算法看着长其实内容还是比较简单的,主要是因为没有各种奇奇怪怪的调用(还有可能是因为Java就是这样的?)不过各种不一样的用法还是要记录一下的
+ 简单读一下,发现flag就是由两部分组成的,用户名和密码
+ String 设置字符串类型的标准,后面的string是变量名(java对大小写敏感)
+ .digest()就是取哈希值
+ View 是 Android 中所有UI组件的基类，比如：
Button、TextView、EditText 都是 View 的子类
相当于网页中的 DOM(交互) 元素
+ .show(用于显示提示消息) ,this是当前互动对象,0是显示时长
+ 对字符串 "zhishixuebao" 做 MD5 摘要然后取偶数位就是用户名,用解码器手动取吧
+ 密码要求长度为15,通过其给出的算法后结果要等于0,那就把它的代码反着打在python上
```
str2=''

for i in range(15):
    str2+=chr((((255-i)+2)-98)-ord('0'))

print(str2)
```
就ok了
## MeowMeowMeow
![alt text](image-4.png)
+ 我预想过的题目真的出现了......当我发现有这个功能的时候,就在想会不会有一天会让我看见这样的music题,这也太浪漫了
## 贝斯是什么乐器啊？
+ 我去,原来是贝斯啊,我还以为是base呢,你看这事闹的哈哈
```
_main();
  puts("please input your flag:");
  scanf("%s", Str);
  for ( i = 0; i < strlen(Str); ++i )
    Str[i] -= i;
  base64_encode(Str2, Str);
  if ( !strcmp(enc, Str2) )
    printf("yes!");
  else
    printf("error");
  return 0;
}
```
+ 好久没见过这么慈祥的代码了
+ 好的,那么首先读取我们的输入,对每一位都减去自己对应的位的数字之后我们就得到了完全体的str
+ 接下来把str扔进base64加密中进行加密,输出了str2
+ 如果是几周之前的我,肯定就把代码喂给ai然后"哦哦哦"地直接启动工具解码了
+ 如果是几天之前的我肯定就傻不愣登地点进那个函数里"啊啊啊?"地研究算法了
+ 但是现在不一样了,我看了几集逆向视频教学,积累了一定经验后发现,这些算法也不一定要看懂啊,很多时候只要知道它是在干啥就好了,我们要研究的只有关键的,卡住我们的部分
+ 一看这个函数名就知道是个base64的加密过程啊,那还研究集贸,之前动用工具就好了
+ 把里面找到的结果提取出来解码后得到NRQ@PAu;8j[+(R:2806.i
+ 这就是str,前面提到过还需要经过每一位都减去自己对应的位的数字的过程才是输入,这种简单的脚本哥们还是会写的
```
enc = "NRQ@PAu;8j[+(R:2806.i"
result = []
for i in range(len(enc)):
    a = enc[i]
    result.append(chr((ord(a) + i)))
print(''.join(result))

```
+ 运行之后结果就出来了
## base2
+ 题目里提示说看看码表呢,那就留意一下吧
```
__int64 __fastcall main(int a1, char **a2, char **a3)
{
  char *s1; // [rsp+18h] [rbp-8h]

  s1 = (char *)sub_124C(a2[1], a2, a3);
  if ( !strcmp(s1, s2) )
    printf("This is flag!!");
  else
    printf("This is wrong");
  free(s1);
  return 0LL;
}
```
+ 进去直奔main函数,又见面了啊,sub
+ 先不必管它,看见老朋友if(!strcmp)了,不用想都知道s1是输入,s2就是加密结果了
+ 跟进去找到s2
![](image-5.png)
+ shift+e提取c variable:GyAGD1ETr3AcGKNkZ19PLKAyAwEsAIELHx1nFSH2IwyGsD==
+ 返回跟进sub函数看一眼,果然是base64加密过程
+ 敲个tab看看汇编形态,找到可疑字符串,来到总页面直接复制NOPQRSTUVWXYZABCDEFGHIJKLMnopqrstuvwxyzabcdefghijklm0123456789+/
+ 启动misc工具包,启动cyberchef,解出
## ezUPX
![alt text](image-6.png)
+ 嗯,那就是upx了
![alt text](image-7.png)
+ 好,IDApro启动吧
![alt text](image-8.png)
+ 考察我的英语水平吗...有点意思,接受挑战!!!以下,是全文的翻译-------
+ ...开玩笑的,直接看重点吧
+ qmemcpy其实就是memcpy,一个复制值的指令罢了
+ 下面给v6的值都异或2和v4对应位置进行比较,不一样就错了,那一样就对了,直接动手吧
```
enc = "LQQAVDyWRZ]3q]zmpf]uc{]vm]glap{rv]dnce"
flag = ""
for i in range(0, len(enc)):
flag += chr(ord(enc[i]) ^ 2)
print(flag)
```
+ 不知道为啥我咋改都搞不出来最后一个},有点抽象了,师傅你要是看见了帮我搞搞呗
## ezTEA
+ aaa脑汁要爆炸嘞,5个月没动过了岂可修
+ 233好了看题,直接先启动pdf,看一眼,我去,查脚本就完蛋啦!!!草,那没办法了,只能自己写了(不过其实自己也写不了几句就是了,全是在原代码里复制过来改的)
+ 这题也没有什么寻找环节,直接把我改完的代码掏出来吧
```
#include <stdio.h>
#include <stdint.h>
void encrypt (uint32_t* v, uint32_t* k) {
    uint32_t v0 = v[0], v1 = v[1];
    uint32_t delta = 0xd33b470;
    uint32_t sum=delta*32;
```
+ 这里就该注意了,与原文不同的是由于sum在这里就要经过一步运算了,所以魔术需要定义在sum之前
```
    for (int i = 0; i < 32; i++) {
        v1-=((v0<<4) + k[2]) ^ (v0 + sum) ^ ((v0>>5) + k[3]);
        v0-=((v1<<4) + k[0]) ^ (v1 + sum) ^ ((v1>>5) + k[1]);
        sum-=delta; }
    v[0] = v0;
    v[1] = v1;
}
```
+ 直接掏过来的加密函数,在考虑其逆向的时候我的思路是想象一个末尾数据,倒着灌回去
+ 则它会依次作为v1倒灌(???怎么怪怪的???),然后减去倒数第二个v1原本加上的数(这个数是不能改变的,也就奠定了括号内部不改变的特性),我在这里为了便于理解做了一点小修改
![alt text](image-9.png)
我直接这么写了,看起来能直观不少
+ 接下来得到修改的v1带着它的新值回去修改(制造)v0的值,然后再减掉一个魔术,得到了我输入的东西的一部分
```
int main() {
    uint32_t k[4] = {1, 2, 3, 4};
    uint8_t input[32] = {0x17, 0x65, 0x54, 0x89, 0xed, 0x65, 0x46, 0x32, 0x3d, 0x58, 0xa9, 0xfd, 0xe2, 0x5e, 
0x61, 0x97, 0xe4, 0x60, 0xf1, 0x91, 0x73, 0xe9, 0xe9, 0xa2, 0x59, 0xcb, 0x9a, 0x99,0xec, 0xb1, 0xe1, 0x7d};
    for (int i = 0; i < 32; i+=8) {
        uint32_t v[2] = {*(uint32_t *)&input[i], *(uint32_t *)&input[i+4]};
        encrypt(v, k);
```
这里取了8字节的值,设定一个大小为2的v数组,分成了两部分作为v[0]和v[1],进入解密函数初始化给v0v1
```
                for (int j = 0; j < 2; j++) {
            uint32_t temp = v[j];
            for (int k = 0; k < 4; k++) {
                printf("%c", temp & 0xFF);
                temp >>= 8;
            }
        
       }
	}
    return 0;
```
最后的返回值弹回了这里,使用嵌套循环分别处理v0v1,作为字符串弹出,然后放到储存器底,继续下一轮循环,达成逆序输出的目的(因为底存表出,所以想得到原文需要逆序)
## ezRC4
+ 工欲善其事必先利其器,先找一篇博客来学习一下RC4吧
+ 欸!!!!那么说到博客就不得不推荐一下我手中的这个超迪奥博客,简直就是精妙绝伦啊,我这辈子都没见过写得这么精彩的博客!!!链接呢已经给屏幕前的家人们打包好了.就在下方
+ https://hanafudastore.github.io/2025/05/26/RC4/
+ 那么根据博客介绍呢,rc4是一种流加密算法(这集我好像在misc的压缩包密码里看过),工作原理是两个部分
+ KSA密钥调度:初始化一个256字节的数组S[i]=i,根据密钥循环混排其中元素
+ PRGA伪随机生成算法(这集也在misc里看过):将打乱后的s数组进行循环更新和交换元素生成伪随机字节再和明文进行异或运算,得到密文
+ 好的,现在咱们了解其加密内容,来看题吧
```
int __fastcall main(int argc, const char **argv, const char **envp)
{
  unsigned __int8 data[27]; // [rsp+20h] [rbp-40h] BYREF
  unsigned __int8 key[17]; // [rsp+40h] [rbp-20h] BYREF
  unsigned int key_len; // [rsp+5Ch] [rbp-4h]
\\创建了个档案数组,密钥数组和密钥长整数(这个整数我写解密接口时就tm写错了)
  _main();
  strcpy((char *)key, "wanyuanshenwande");
  key_len = 16;
  puts("what is this?");
  data[0] = -21;
  data[1] = 13;
  data[2] = 97;
  data[3] = 41;
  data[4] = -65;
  data[5] = -101;
  data[6] = 5;
  data[7] = 34;
  data[8] = -13;
  data[9] = 50;
  data[10] = 40;
  data[11] = -105;
  data[12] = -29;
  data[13] = -122;
  data[14] = 77;
  data[15] = 45;
  data[16] = 90;
  data[17] = 42;
  data[18] = -93;
  data[19] = 85;
  data[20] = -86;
  data[21] = -43;
  data[22] = -76;
  data[23] = 108;
  data[24] = -117;
  data[25] = 81;
  data[26] = -79;
  rc4_crypt(data, 0x1Bu, key, 0x10u);
  \\全是填数据的操作,然后放进crypto里操作,跟进吧
  putchar(10);\\输出单个ascil码为10的字符,就是换了个行
  return 0;
}
```
+ 直接跟进
```
void __cdecl rc4_crypt(unsigned __int8 *Data, unsigned int Len_D, unsigned __int8 *key, unsigned int Len_k)
{
  __int64 v4; // [rsp+0h] [rbp-80h] BYREF
  unsigned __int8 s[256]; // [rsp+20h] [rbp-60h]
  unsigned __int8 tmp; // [rsp+12Fh] [rbp+AFh]
  int t; // [rsp+130h] [rbp+B0h]
  unsigned int k; // [rsp+134h] [rbp+B4h]
  int j; // [rsp+138h] [rbp+B8h]
  int i; // [rsp+13Ch] [rbp+BCh]

  rc4_init((unsigned __int8 *)&v4 + 32, key, Len_k); //继续跟进
  i = 0;
  j = 0;
  t = 0;
  for ( k = 0; k < Len_D; ++k )
  {
    i = (i + 1) % 256;
    j = (j + s[i]) % 256;
    tmp = s[i];
    s[i] = s[j];
    s[j] = tmp;
    t = (unsigned __int8)(s[i] + s[j]);
    Data[k] ^= s[t];                           //发现加密算法,那么打乱环节在哪呢?,上面不是有个可疑的函数吗
  }
}
```
+ 好,进入rc4_init
```
void __cdecl rc4_init(unsigned __int8 *s, unsigned __int8 *key, unsigned int Len_k)
{
  char k[256]; // [rsp+0h] [rbp-80h] BYREF
  unsigned __int8 tmp; // [rsp+107h] [rbp+87h]
  int j; // [rsp+108h] [rbp+88h]
  int i; // [rsp+10Ch] [rbp+8Ch]

  j = 0;
  memset(k, 0, sizeof(k));
  tmp = 0;
  for ( i = 0; i <= 255; ++i )//发现初始化
  {
    s[i] = i;
    k[i] = key[i % Len_k];//发现密钥处理
  }
  for ( i = 0; i <= 255; ++i )
  {
    j = (k[i] + j + s[i]) % 256;
    tmp = s[i];
    s[i] = s[j];     //发现打乱
    s[j] = tmp;
  }
}
```
至此,所有函数都找到了,那么拼在一起写个解密脚本吧
```
#include <stdio.h>
#include <string.h>
void __cdecl rc4_init(unsigned __int8 *s, unsigned __int8 *key, unsigned int Len_k)
{
  char k[256]; // [rsp+0h] [rbp-80h] BYREF
  unsigned __int8 tmp; // [rsp+107h] [rbp+87h]
  int j; // [rsp+108h] [rbp+88h]
  int i; // [rsp+10Ch] [rbp+8Ch]

  j = 0;
  memset(k, 0, sizeof(k));
  tmp = 0;
  for ( i = 0; i <= 255; ++i )
  {
    s[i] = i;
    k[i] = key[i % Len_k];
  }
  for ( i = 0; i <= 255; ++i )
  {
    j = (k[i] + j + s[i]) % 256;
    tmp = s[i];
    s[i] = s[j];
    s[j] = tmp;
  }
}
void __cdecl rc4_crypt(unsigned __int8 *Data, unsigned int Len_D, unsigned __int8 *key, unsigned int Len_k)
{
  __int64 v4; // [rsp+0h] [rbp-80h] BYREF
  unsigned __int8 s[256]; // [rsp+20h] [rbp-60h]
  unsigned __int8 tmp; // [rsp+12Fh] [rbp+AFh]
  int t; // [rsp+130h] [rbp+B0h]
  unsigned int k; // [rsp+134h] [rbp+B4h]
  int j; // [rsp+138h] [rbp+B8h]
  int i; // [rsp+13Ch] [rbp+BCh]

  rc4_init(s, key, Len_k);
  i = 0;
  j = 0;
  t = 0;
  for ( k = 0; k < Len_D; ++k )
  {
    i = (i + 1) % 256;
    j = (j + s[i]) % 256;
    tmp = s[i];
    s[i] = s[j];
    s[j] = tmp;
    t = (unsigned __int8)(s[i] + s[j]);
    Data[k] ^= s[t];
  }
}
int main(){
	unsigned char key[17];
	unsigned int key_len;
	unsigned char data[27];
	strcpy((char*)key,"wanyuanshenwande");
	key_len=16;
  data[0] = 0xEB;   // -21 -> 0xEB
  data[1] = 0x0D;   // 13
  data[2] = 0x61;   // 97
  data[3] = 0x29;   // 41
  data[4] = 0xBF;   // -65 -> 0xBF
  data[5] = 0x9B;   // -101 -> 0x9B
  data[6] = 0x05;   // 5
  data[7] = 0x22;   // 34
  data[8] = 0xF3;   // -13 -> 0xF3
  data[9] = 0x32;   // 50
  data[10] = 0x28;  // 40
  data[11] = 0x97;  // -105 -> 0x97
  data[12] = 0xE3;  // -29 -> 0xE3
  data[13] = 0x86;  // -122 -> 0x86
  data[14] = 0x4D;  // 77
  data[15] = 0x2D;  // 45
  data[16] = 0x5A;  // 90
  data[17] = 0x2A;  // 42
  data[18] = 0xA3;  // -93 -> 0xA3
  data[19] = 0x55;  // 85
  data[20] = 0xAA;  // -86 -> 0xAA
  data[21] = 0xD5;  // -43 -> 0xD5
  data[22] = 0xB4;  // -76 -> 0xB4
  data[23] = 0x6C;  // 108
  data[24] = 0x8B;  // -117 -> 0x8B
  data[25] = 0x51;  // 81
  data[26] = 0xB1;  // -79 -> 0xB1
  rc4_crypt(data,0x1B,key,0x10);
  for(int i=0;i<27;i++){
  	printf("%c",data[i]);
  }
  return 0;
}
```
+ 纯纯拼好本,给我写笑了都
+ 嗯,事情到这里,其实还没有结束,我忽然想到之前一个迷宫题,在开头对迷宫做出了很复杂的变换,我都快做绝望了,去请教一个前辈,他给我说"这时候就该考虑动态调试了"
+ 那么实际上的动态调试并不是一种特定的题型,而是在这个可执行文件中得到间数据的一种手段,大部分动态调试题内部可能是损坏的,无法直接运行
+ 但是这道题我直接拼好本就能把答案拼出来,那么全代码肯定是没有问题的,那我为什么不直接试着得到末数据instead of间数据呢?
+ 再者根据博客里的大手子的介绍,rc4有完全的自反性,又十分恰好的这道题是一个正向方向,连数据位置都不用改,那我还写集贸啊?
+ 我直接启动动态调试
![alt text](image-11.png)
吼吼哈哈111
# 问答环节
## UPX如何识别？如何脱壳？如何加壳？
+ 直接放进逆向工具包里看看实力
+ 把文件放进upx文件里,cd到文件内部然后upx -d 文件名.后缀
+ 加壳去掉-d
## Base64编码的特征？不用工具怎么解？逆向程序时如何识别？有什么变体？
+ 存在其码表里的字符A-Za-z0-9+/,通常以==结尾(用作补位),长度是4的倍数,不包含特殊字符
+ 1一个优秀的脚本小子不能失去他的工具🤯
+ 2就算失去了我直接在python里用import base64让它给我一键输出!别想让我花时间算!
```
import base64
enc='aGFuYWZ1ZGE='
flag=base64.b64decode(enc)
print(flag.decode())
```
+ 3实在不行我在网页的url里输入data:text/plain;base64,aGFuYWZ1ZGE=不是照样中?
+ 逆向程序中会有一个包含如上字符的编码表,我直接搜索码表!有的话找到加密函数重命名成base64_encode
+ 1 变表base64
+ 2 其他base家族
+ 3url安全base64(省略=,+/换成-_)
## RC4、TEA算法的实现过程？逆向中如何识别？本地给自己留一份代码脚本
+ 过程在前面的解题内容里逐步描述了
+ rc4首先要注意它的256字节s盒,还有它的key,key长都会被用到,其次是其两层嵌套循环加密,即init包在crypot里,还有扔在最后的异或操作,要素齐全就是它没跑了
+ TEA的截断两部分互相处理很有特征,比较好认,还有那个魔数,常常是黄金分割率的变形0x9E3779B9,还有多移位操作,多key,每次处理8字节都是特征,我们常常关注特征同时出现,除非有某个特性特征(原来是在玩恐鬼症啊,这下看懂了)
+ 一个优秀的脚本小子完全不用提醒,肺雾专用小脚本多的是😎😎😎
# 杂项</font>
+ 终于把我心心念念了好久的虚拟机搞了,顺便连到了vscode上